interface:
  display_name: "Code Review"
  short_description: "Review new code against project conventions"
  default_prompt: "Use $code-review to review the new code against this project's style and quality rules."

policy:
  allow_implicit_invocation: true
